export default{translation:{structure:'The {{ type }} `{{ name }}` is not found.',dependency:{required:'Add `{{ name }}` to `{{ type }}` in the package.json file.',extra:'Remove `{{ name }}` from `{{ type }}` in the package.json file.'},packageSettings:'The `{{ name }}` field is not filled in the package.json file.',eslint:'Run the command `npx eslint .` and fix the errors in the code: {{- errors }}',eslintrc:{extends:'Add `extends: "{{ value }}"` in the .eslintrc file.',rules:'Add rule `{{ name }}: "{{- value }}"` in the .eslintrc file.'}}};